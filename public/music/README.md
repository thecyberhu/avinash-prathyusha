Place your MP3 file here so it is served at `/music/radhe-sham.mp3`.

Steps:
1. Copy your song file and rename it to `radhe-sham.mp3`.
2. Put the file in this directory (`public/music/radhe-sham.mp3`).
3. Run your dev server (e.g., `npm run dev`) and open the site.

Notes:
- The site attempts to autoplay the track muted on load. Due to browser autoplay policies, the audio will start muted; tapping/clicking anywhere will unmute and play audibly.
- If you prefer the file to be bundled instead of served from `public/`, tell me and I'll update the import and code accordingly.
